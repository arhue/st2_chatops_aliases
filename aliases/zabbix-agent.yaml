---
name: chatops.zabbix-agent
action_ref: st2_chatops_aliases.zabbix-agent
description: zabbix-agent
formats:
  - display: "do zabbix-agent <process=status> <project=instantpay> <hosts=web> "
    representation:
      - "do nginx {{ process }} {{ project }} {{ hosts=web }}"
result:
  format: |
    Here is top for `{{ execution.parameters.hosts }}` host(s): {~}
    ```{{ execution.result.stdout }}```
