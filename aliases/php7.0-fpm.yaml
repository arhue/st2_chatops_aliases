---
name: chatops.php7.0-fpm
action_ref: st2_chatops_aliases.php7.0-fpm
description: php7.0-fpm
formats:
  - display: "do php7.0-fpm <process=status> <project=instantpay> <hosts=web> "
    representation:
      - "do nginx {{ process }} {{ project }} {{ hosts=web }}"
result:
  format: |
    Here is top for `{{ execution.parameters.hosts }}` host(s): {~}
    ```{{ execution.result.stdout }}```
